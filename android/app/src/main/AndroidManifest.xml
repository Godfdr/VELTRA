<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.NFC"
        android:required="true" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC"
        android:required="true" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Optional Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Hardware Features -->
    <uses-feature android:name="android.hardware.nfc" android:required="true" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.fingerprint" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_veltra_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VELTRA"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Splash / Intro -->
        <activity
            android:name=".VeltraSplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".VeltraOnboardingActivity" />
        <activity android:name=".VeltraLoginActivity" />
        <activity android:name=".VeltraSignUpActivity" />

        <!-- Dashboard -->
        <activity android:name=".VeltraMainActivity" />

        <!-- Payment & Send Money -->
        <activity android:name=".VeltraSendMoneyStep1Activity" />
        <activity android:name=".VeltraSendMoneyStep2Activity" />
        <activity android:name=".VeltraPaymentSuccessActivity" />
        <activity android:name=".VeltraReceiveActivity" />
        <activity android:name=".VeltraNFCPaymentActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />
        </activity>

        <!-- Pockets & Wallets -->
        <activity android:name=".VeltraWalletsActivity" />
        <activity android:name=".VeltraOfflineWalletActivity" />
        <activity android:name=".VeltraTapAndPayActivity" />
        <activity android:name=".VeltraCardsActivity" />

        <!-- Bills & Services -->
        <activity android:name=".VeltraPayBillsActivity" />
        <activity android:name=".VeltraTransactionDetailsActivity" />

        <!-- User & Admin -->
        <activity android:name=".VeltraProfileActivity" />
        <activity android:name=".VeltraTopUpMethodActivity" />
        <activity android:name=".VeltraAgentTopupActivity" />
        <activity android:name=".VeltraAdminDashboardActivity" />
        <activity android:name=".VeltraAnalyticsActivity" />
        <activity android:name=".VeltraContactlessActivity" />
        <activity android:name=".VeltraSavingsActivity" />
        <activity android:name=".VeltraBillSplitActivity" />

    </application>

</manifest>
